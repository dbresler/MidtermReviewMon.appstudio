/*
Do the same program as #1 but using 
controls and events (no prompts, alerts, etc.).
*/

/*
btnCalc.onclick=function(){
  let sum = Number(inptNumber1.value) + Number(inptNumber2.value)
  lblResult.value = `The sum of the numbers is ${sum}`
  }
  
  */
  